# 班级管理：前端 v29 / 云函数 v22

本次是在已可用的学生管理上增加班级，不修改管理员账号、学生邀请码或课程材料。没有向线上写入测试名单。

## 部署顺序

1. 原来的云函数上传 `CCMTEACHER-deploy-v22-classes.zip`，执行方法仍为 `index.main_handler`，Node.js 20。ZIP根目录直接包含index.js、students.js和依赖，没有src外层。
2. 等云函数更新成功后，把本地 `GITHUB发布页/` **里面的内容**上传到CCM仓库根目录；同名覆盖，其余原有文件保留。也可解压 `CCM-FRONTEND-2026-09-03-v29.zip` 后上传。
3. 刷新网页（必要时Ctrl+F5），管理员进入“系统设置 → ④学生管理”。如果提示需要v22，说明云函数还没有更新成功或前后端版本不匹配。

函数地址、环境变量、COS权限与v21完全相同，详见 `STUDENT_MANAGEMENT_DEPLOYMENT.md`。不用新建桶、目录或手工上传JSON。PWA图标和安装逻辑保持不变。

## 日常操作

- **新增班级**：填写班级名称并保存，自动进入新班。名称中英文均可，不得与其他班级重名。
- **新增/导入学生**：先选择班级，新增或导入的学生自动加入当前班级。模板仍是学号、中文名、英文名三列。模板有两行示例，上传前替换或删除。
- **现有学生分班**：进入“未分班”，勾选学生 → 选择“目标班级” → “确认分班”。改名、转班不会改变学生邀请码或历史成绩。
- **一人一班**：学号仍在整个课程平台唯一。已经存在的学生应转班，不能在另一个班重复导入。
- **归档/恢复**：归档不删除班级或学生，也不会停用登录。归档后不能向该班新增/导入/转入；可以转出学生。恢复后继续使用。需要停止某位学生登录时仍用“停用学生”。
- **登录邀请**：复制信息包含班级名称。复制本组邀请只包含当前组启用的学生，但不受姓名搜索影响；注意私发，不要把整班邀请码发到群聊。
- **成绩/看板**：选择班级筛选行为和测验，CSV增加班级列。转班后，过去的成绩和行为按学生现在所在的班级显示；不是按作答当时的历史班级分账。“全部班级”行为汇总保留管理员活动，指定班级只显示该班学生活动；测验一律排除管理员试答。

默认显示“全部学生”，此时不能直接新增/导入，须先选一个班级（或未分班）。切换班级清空尚未提交的学生表单、导入预览和勾选项。可以一次勾选多人分班。

## 数据与升级

继续使用同一个 `system/student-roster-v1/revisions/` 私有版本快照。新版快照增加 `schemaVersion: 2`、`classes` 数组和学生的 `classId`。班级内部ID固定，重命名不影响关联。

v21创建的学生未带班级，加载时自动视作“未分班”，不立即写入或生成新账号。修改班级/学生时才保存下一版本。保留现有并发冲突保护；冲突或网络中断后先刷新再重试。

成绩仍在 `system/quiz-results-students-v1/`，按学号关联，不移动或重写成绩文件。

**回滚**：保留v28/v21旧包，但不要使用更早的v20学生登录逻辑；它读取旧测试账号。如果出现问题，先保留COS最新快照和报错，不要手工删除班级或清空名单。

## 本地检查

- `node tools/test-students.cjs`：旧名单读时升级不写入、班级名称规范化/重名拦截、跨班重复学号拒绝、批量转班原子性、归档限制、恢复、管理员权限、班级过滤、转班后账号及分数/首次作答锁定保留。
- JavaScript语法、内联脚本与PWA回归检查通过。
- 浏览器使用真实名单服务代码＋本地内存COS替身验证班级创建、模板解析/预览/确认导入、班级重命名；不接触生产学生数据。
- 部署后仍需用真实学生做一次建班/分班和按班下载验收。
