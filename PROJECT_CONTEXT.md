# CCM 多语言学习平台项目上下文

最后更新：2026-09-03

## 1. 项目概述

本项目是“跨文化管理课程多语言学习支持系统”的学生端与教师管理端。前端以单页静态网站形式运行，主要部署到 GitHub Pages；腾讯云对象存储（COS）保存课程材料、权限配置、登录账号、测验结果和行为数据；腾讯云函数（SCF）负责鉴权及对 COS 的受控读写。

当前平台覆盖：

- 第 1–6 章、期末任务、关于课程及 AI 工具与 APPs。
- 多语言课件、练习与任务、视频案例及 SRT 字幕。
- 学生/管理员登录与权限控制。
- 管理员材料目录、开放/关闭设置、按班级筛选的用户行为看板和班级/学生管理。
- 第 2 章 2.1–2.6 视频分段闯关测验。
- 跨文化情景角色对话智能体的纯前端原型。

## 2. 当前版本与可信基线

### 当前前端

- 版本：`frontend-2026-09-03-v30`（多语言JSON字幕，本地已实现；本次未替用户发布到线上）
- 当前源文件：`index.html`
- 部署包：`CCM-FRONTEND-2026-09-03-v30.zip`
- GitHub发布文件夹：`GITHUB发布页/`；只上传该文件夹内的内容到仓库根目录，不要上传文件夹本身这一层。
- 内联脚本检查工具：`tools/check-inline-scripts.js`

### 当前云函数

- 部署包：`CCMTEACHER-deploy-v23-json-subtitles.zip`（本地新包，线上部署仍需用户操作）
- SCF 执行方法：`index.main_handler`
- 运行环境：Node.js 20
- 部署包根目录必须直接包含 `index.js`、`package.json` 和依赖，不能再多包一层 `src/`。

### 历史基线说明

`STABLE_RELEASE.md` 记录的是较早的 `stable-2026-07-14-v13` 管理功能基线，并非当前完整版本。v26 是智能体原型，v27 是PWA，v28/v21 是学生管理（用户确认可用），当前 v29/v22 新增班级管理。以后排错或回滚时，应先备份线上 COS 系统文件，并根据故障范围选择版本，不应直接把 v13 当作最新版本。

### 重要文件差异

- 根目录 `index.html`：当前开发与部署源，版本 v30。
- `github/index.html`：2026-06-29 的旧副本，未包含后续登录、材料管理、测验、看板和智能体原型更新，不能用于覆盖当前线上首页。

## 3. 系统架构

```text
GitHub Pages 静态前端
        |
        | HTTPS / JSON
        v
腾讯云函数 CCMTEACHER
        |
        | COS SDK
        v
腾讯云 COS：ccm2025-1311686407（ap-chongqing）
```

### 前端接口与资源地址

- 云函数接口：`https://1311686407-1ngfs1nlvf.ap-guangzhou.tencentscf.com`
- COS 公共资源根：`https://ccm2025-1311686407.cos.ap-chongqing.myqcloud.com/`
- 材料目录：`system/materials.json`
- 系统设置：`system/settings.json`
- 管理员账号：`system/adminPIN.json`
- 学生账号：`system/studentPIN.json`

注意：云函数访问地址包含 `ap-guangzhou`，但课程 COS 桶实际区域为 `ap-chongqing`。两者不要混淆。

## 4. 腾讯云函数配置

推荐环境变量：

```text
COS_BUCKET=ccm2025-1311686407
COS_REGION=ap-chongqing
SETTINGS_KEY=system/settings.json
MATERIALS_KEY=system/materials.json
ADMIN_PIN_KEY=system/adminPIN.json
STUDENT_PIN_KEY=system/studentPIN.json
```

云函数角色至少需要：

```text
cos:GetObject
cos:PutObject
cos:GetBucket
```

其中 `cos:GetBucket` 用于按前缀读取行为数据和测验结果。

不要在本文档、前端代码或公开 GitHub 仓库中记录真实邀请码。账号 JSON 当前使用如下结构：

```json
{
  "users": [
    {
      "id": "用户ID",
      "inviteCode": "邀请码"
    }
  ]
}
```

## 5. COS 系统数据

主要对象与目录：

```text
system/adminPIN.json
system/studentPIN.json
system/settings.json
system/materials.json
system/history/
system/analytics/
system/quiz-results/
```

- `settings.json`：平台模块及旧版资源开关，继续作为兼容配置使用。
- `materials.json`：当前材料目录树，是材料名称、顺序、状态、资源类型、语言和 COS 地址的主要数据源。
- `history/`：材料目录等系统数据的历史备份。
- `analytics/`：登录、章节访问、材料打开等行为事件。
- `quiz-results/`：学生视频闯关题的首次作答结果。

本地 `system/materials.json` 是由早期前端生成的目录样本；线上管理操作以后，应以 COS 中的最新版本为准，修改前先读取线上数据，避免本地旧文件覆盖云端新目录。

## 6. 已完成并验证的功能

### 6.1 登录与权限

- 学生和管理员账号均通过云函数读取 COS 中的 PIN JSON 进行验证。
- 原有前端硬编码/旧 pincode 链接已停用。
- 未登录状态只开放登录、关于这门课、AI 工具与 APPs。
- 学生登录后按系统设置开放相应章节与材料。
- 管理员登录后才显示“系统设置”。
- 登录后页面顶部显示登录者姓名与身份，并提供退出登录按钮。
- 退出后清理当前登录状态、关闭悬浮窗口并恢复未登录权限。

### 6.2 系统设置

系统设置窗口支持拖动、拉大缩小、全屏、恢复和关闭，并分为三个模块：

1. 文件与排序
2. 开放与关闭
3. 用户行为看板

文件与排序已经实现：

- 以目录树显示章节、功能区、材料及其资产。
- 显示学生端可见名称，而不仅是 COS 原始文件名。
- 重命名材料。
- 上移/下移调整顺序。
- 重命名和排序后保持目录树展开状态。
- 管理操作保存后与学生端联动。

开放与关闭已经实现：

- 可按章节、功能区、材料或单个文件控制发布状态。
- 关闭的内容在学生端直接隐藏，不显示灰色名称。
- 父子节点状态联动。
- 保存后写入 COS，学生端刷新后生效。

### 6.3 材料管理

- 可登记已经上传到 COS 的视频地址，适合较大的 MP4 视频。
- 可替换已登记视频的 COS 地址。
- 可删除视频材料。
- 可上传、替换 SRT 字幕文件。
- 可登记、替换或删除普通文档材料。
- 新增、替换和删除后，学生端能够同步读取更新。
- 实际使用中视频统一建议转换为 MP4；WMV 浏览器兼容性不足。
- SRT 解析器已经兼容没有空行分隔、连续编号的字幕文件，解决了部分视频只加载一条字幕的问题。

大视频当前采用“先上传 COS，再在系统设置中登记链接”的路线，不经云函数转发完整视频文件，避免云函数请求体、超时和内存限制。SRT 等小文件可直接由前端提交给云函数处理。

### 6.4 学生端材料体验

- 课件/PDF 预览。
- PPT/PDF 显示区支持全屏、恢复和缩回。
- 视频悬浮窗口支持移动和拉大。
- 已修复视频窗口拖到页面顶部后标题栏不可再触及的问题。
- 视频支持多语种 SRT 字幕，并可根据材料目录动态更新。
- 管理员关闭的材料在学生端隐藏。

浏览器控制台中的 `cos-document-preview-sdk` 和 `fileOpen cost` 属于腾讯 COS 文档预览 SDK 的正常日志；只有伴随实际预览失败时才需要排查。

### 6.5 第 2 章视频闯关测验

视频 2.1–2.6 已完成分段闯关题：

- 每个视频 8 道中英文对照选择题。
- 每题 1 分。
- 视频播放到卡点后自动暂停。
- 学生必须完成当前题目才能继续向后播放。
- 允许回看之前的视频内容，但不能越过尚未完成的卡点。
- 首次答案写入 COS 并锁定，不能重新作答。
- 学生可以回看已经完成题目的答案与解析。
- 页面重新加载后可从云端读取作答进度。
- 原问卷星测试入口暂时保留。

### 6.6 用户行为看板

看板已使用共享显示区和子导航呈现，包含：

- 总览
- 测试结果
- 题目统计
- 活动记录

现有能力：

- 统计登录、章节访问、材料打开等事件。
- 查看全部学生的各视频测试结果。
- 显示完成度、得分和题目表现。
- 使用彩色卡片、条形图和环形图展示。
- 下载测试统计 CSV。

### 6.7 其他前端功能

- 关于课程页面。
- AI 工具与 APPs 导航区。
- 章节课件、练习与任务、视频案例的共享显示布局。
- 多语言课件和字幕，包含中文/英文以及泰语、印尼语、老挝语等已有资源。

## 7. 跨文化情景智能体原型

### v27 新增：PWA 安装（与智能体功能独立）

- 顶部“安装应用 / Install app”入口，登录前后均可使用。
- 支持 `beforeinstallprompt` 的浏览器可由用户点击调用原生安装窗口；取消后给出中英提示。
- iPhone/iPad 显示 Safari 分享→添加到主屏幕的中英文说明；其他浏览器显示菜单安装指引。
- 已安装窗口通过 standalone 等显示模式识别，并隐藏入口；普通浏览器标签不能可靠判断所有已有安装，因此仍可能显示安装帮助。
- 图标采用已确认的圆形蓝色科技地球，有普通透明版、maskable 安全区版及 Apple 图标。
- 新增 `manifest.webmanifest`、`assets/pwa.js`、`assets/pwa.css`、`sw.js`、`offline.html` 和 `assets/icons/pwa/`。
- Service Worker 只缓存中立的离线提示页；主页面每次联网获取，不缓存账号、权限、成绩、材料目录或视频。原有登录本地存储机制未改动。
- 不强制刷新正在答题的页面，不自动弹出安装窗口。
- 通过语法检查、自动化状态/缓存隔离测试和本地浏览器双语弹窗检查；尚需在正式 HTTPS 页面及真实手机上验收原生安装。
- 部署时不能只上传首页，详见 `PWA_DEPLOYMENT.md`；云函数不需要更新。

### 智能体原型状态（保持 v26 行为）

前端 v26 新增了纯前端原型，入口位于登录后页面顶部：

- 使用“地球＋对话框”图标代表跨文化情景智能体。
- 未登录不显示；登录后以低调的隐藏式按钮出现，悬停时展开名称。
- 点击打开可移动、可拉大缩小、可全屏、可恢复、可缩回、可关闭的悬浮窗口。
- 内容区支持滚动。

原型导航：

- A：跨文化团队类型
  - 象征性成员团队
  - 双文化团队
  - 多文化团队
- B：跨文化融合
  - 吉利 × Volvo
  - 福耀美国工厂
  - 联想 × IBM
- C/D/E：暂时保留为待建设主题。

原型学习流程：

```text
背景材料
→ 前半段视频
→ 角色会议
→ 提交决策
→ 后半段视频
→ 反思报告
```

原型还包括：

- 示例角色、内置头像和中英双语角色信息。
- 中文、英文及中英双语界面/讨论/反馈选择器。
- 学生导入头像，压缩为 256×256 WebP 后保存在当前浏览器本地。
- 角色发言草稿演示。

尚未接入：

- COS 案例背景和前后半段视频。
- 真实大模型/智能体回复。
- 云端对话记录、评分和教师看板统计。
- COS 学生头像上传。
- 正式的角色分配、决策规则和反思量表。

当前示例消息只是界面演示，不能当作已经完成的教学案例内容或真实 AI 输出。

## 8. 云函数现有动作

v20 部署包中已确认的主要动作：

```text
login
get-materials-admin
save-materials-catalog
register-video-link
upload-video-subtitle
replace-registered-video
remove-video-material
register-document-link
remove-document-material
track-event
submit-quiz-answer
get-quiz-progress
get-analytics
```

修改前端调用时必须保持动作名称和请求字段兼容。新增智能体功能时建议使用新的动作名称，不要复用材料或测验接口。

## 9. 关键文件

```text
index.html
    当前完整前端，所有主要 HTML/CSS/JavaScript 均在此文件内。

system/materials.json
    本地材料目录样本；线上以 COS 最新文件为准。

tools/check-inline-scripts.js
    检查 index.html 中经典内联脚本语法。

tools/build-materials-catalog.js
    从既有前端资源辅助生成材料目录。

CCM-FRONTEND-2026-07-19-v26.zip
    当前前端部署包。

CCMTEACHER-deploy-v20-compatible.zip
    当前腾讯云函数兼容部署包。

STABLE_RELEASE.md
    历史 v13 稳定基线记录，不是当前完整版本说明。

备份版/
    已保留的稳定部署包备份。
```

`adminPIN.json` 和 `studentPIN.json` 是本地账号样本，属于敏感配置。若仓库公开，应避免提交真实用户和邀请码，线上以 COS `system/` 目录内文件为准。

## 10. 部署与验证流程

### 前端

1. 以根目录 `index.html` 为唯一当前源文件。
2. 运行：

   ```text
   node tools/check-inline-scripts.js index.html
   ```

3. 更新版本常量 `PLATFORM_VERSION`。
4. 部署到 GitHub Pages。
5. 使用管理员和学生账号分别验证权限、材料目录、视频、字幕和测验。

### 云函数

1. 部署包根目录确认存在 `index.js`。
2. 执行方法保持 `index.main_handler`。
3. 确认环境变量指向 `ccm2025-1311686407` 和 `ap-chongqing`。
4. 更新后分别测试登录、读取材料、保存目录、行为看板和测验结果。
5. 若腾讯云报告 `zip code format error`、`filename not matched: index.js` 或入口文件错误，优先检查 ZIP 是否多了一层目录，不能通过修改业务代码掩盖打包结构问题。

## 11. 修改原则

- 不要把邀请码、SecretId、SecretKey 或其他凭证写入前端。
- 先读取 COS 最新材料目录，再进行管理端保存。
- 保持学生端“关闭即隐藏”的既定行为。
- 保持测验首次作答锁定和不可向前跳过卡点的规则。
- 大视频继续采用 COS 直传/人工上传后登记链接的方式。
- 字幕和小型配置文件可以通过云函数处理。
- 修改悬浮窗口时必须保证标题栏始终可触达。
- 每次发布前运行内联脚本检查，并保留可回滚部署包。
- 如果只修改前端，不要重新部署云函数；如果只修改云函数，不要覆盖前端。

## 12. 建议的下一阶段

### 多语言JSON字幕（v30 / v23，当前最新）

- 视频登记新增可选字幕JSON链接和“添加到首位”。第一章登记对话框提供“0. 盈塑工业（YingSu）”名称、视频及JSON链接的预填按钮；需管理员部署后验证登记并开放，未直接写生产目录。
- 所有视频有JSON字幕链接管理入口：登记、更换、移除JSON字幕引用。COS文件不删除，材料目录仍保留备份与revision校验。
- 每语种保存为type=subtitle的独立目录项，subtitleFormat=json、jsonLanguage记录来源键，url指向同一个JSON；前端subtitleFormats映射区分SRT/JSON，仍可逐语种改名、排序、开放/隐藏。
- JSON替换覆盖同语种字幕、保留它们原有label/status，移除旧JSON已不存在语种，保留其他语种SRT。新语种默认published；新视频仍hidden。单独上传SRT会把该语种subtitleFormat改为srt。
- 新解析器源：cloud-functions/CCMTEACHER/subtitle-json.js，浏览器镜像assets/subtitle-json.js（保持字节一致）。服务器通过COS SDK验证同桶永久JSON链接，拒绝system目录、签名链接等；最大5MB/30语种/30000条，时间顺序与无重叠验证。
- 学生播放器每窗口缓存一次JSON读取，语言切换有请求序号保护避免串字幕，JSON文字用textContent显示，默认使用JSON声明语言（已隐藏时选可用语种）。支持zh→ZH、ko→KR、ms→MY等代码映射。
- 盈塑实际JSON已验证：6语种×161条，359.2秒，默认中文；视频URL与字幕URL均在 `JSON_SUBTITLES_DEPLOYMENT.md`。前端与云函数均需更新，无新npm依赖。
- 本地测试tools/test-subtitle-json.cjs以及学生/班级/PWA回归通过。线上登记和音画同步验收仍需用户部署后操作。

### 2026-09-03 班级管理更新（v29 / v22，已确认可用）

- 学生管理采用班级目录：全部学生、未分班、各班（含归档班）。新增/重命名/归档/恢复班级；每班显示人数。
- 一人一班。班级使用随机UUID固定编号，名称可以修改。学号继续全平台唯一，允许同名学生，不允许跨班重复建相同学号。
- 先选班级，再手动新增/导入学生，模板仍为原三列；导入预览显示目标班级，切换班级会清空未提交预览，避免误入其他班。
- 老v21正式学生无classId时视为未分班；只读加载不会写文件或重新生成账号，第一次修改保存到下一份私有版本快照。同一份快照同时保存classes和students，继续防止并发覆盖。
- 勾选学生后选择目标班级即可批量转班，或移回未分班；保留学号、邀请码、启停状态和历史成绩。
- 归档只整理班级，不停用学生、不删除成绩；不能向归档班级新增或转入，可以转出，恢复后可继续新增。
- 看板的班级筛选同时作用于行为记录和测试表/图表/CSV；按学生当前班级归属关联历史记录。全部班级保留原行为统计口径（含管理员），特定班级仅统计该班学生。成绩仍排除管理员试答。
- 复制单人或当前分组邀请附带班级名称；复制分组包括当前组全部启用学生，不受搜索框影响，会弹出确认说明。
- 后端动作均在students-管理员鉴权入口下：students-class-add/edit/archive、students-transfer；沿用原桶、名单路径、环境变量和权限，无需新增COS文件。
- 后端/导入解析自动测试、浏览器建班、Excel预览与导入、班级重命名测试通过；尚未部署到生产环境。详见 `CLASS_MANAGEMENT_DEPLOYMENT.md`。

### 2026-09-03 学生管理更新（v28 / v21，优先于下文旧版本说明）

- 系统设置第四页为学生管理：手动新增、XLSX/CSV模板导入预览、搜索、编辑姓名、停用/恢复、重置邀请码、单人/全部复制双语登录信息。
- 学号是唯一标识，按文本保留前导零，全角转半角、英文字母统一大写；中文/英文姓名至少填一个，重复姓名允许，重复学号拒绝。
- 邀请码云端安全随机生成，6位1–9、无0、当前名单内唯一。课程“跨文化管理 / Cross-cultural Management”，链接 `https://steven2025.github.io/CCM/`。
- 不兼容旧测试学生账号；不删除旧数据。学生登录改读 `system/student-roster-v1/revisions/` 私有快照，不再读 studentPIN。adminPIN 保持不变。
- 正式学生成绩/行为分别使用 `system/quiz-results-students-v1/`、`system/analytics-students-v1/`，写入私有对象；按学号关联当前姓名。看板表格及CSV增加姓名列。
- 云函数源文件在 `cloud-functions/CCMTEACHER/index.js` 和 `students.js`，发布包直接根目录执行 `index.main_handler`。
- 名单保存使用不可覆盖版本快照，依赖 `cos:GetBucketVersioning` 权限与未启用过版本控制的桶；不满足时明确拒绝保存，不自行改桶配置。
- 新前端身份缓存 `ccm_auth_user_v2` 强制重新登录；头像按学号分隔。
- Excel 解析库及模板复用点点汉语已用文件，位于 `assets/vendor/` 和 `assets/templates/`；模板两行示例需替换或删除。没有改动点点汉语工程。
- 详细部署/权限/验收见 `STUDENT_MANAGEMENT_DEPLOYMENT.md`。本地自动检查和新增/编辑界面模拟测试通过；实际云端/Excel上传导入端到端仍待用户部署验收。
- PWA服务工作线程仍使用v27的离线说明缓存，无需缓存学生资料；前端发布版本为v28。

跨文化情景智能体建议按一个案例纵向完成，而不是同时铺开全部主题：

1. 选择一个跨文化融合案例。
2. 确定教学目标、角色卡、决策点和评分量表。
3. 在 COS 建立案例配置、背景材料及前后半段视频。
4. 新增云函数接口读取案例配置并保存会话/决策。
5. 接入受场景约束的智能体，而不是开放式通用聊天。
6. 把决策质量、理论应用、参与度和反思结果接入教师看板。
7. 完成一个案例的学生端测试后，再复制到另外两个案例。
