# 学生管理部署说明（前端 v28 / 云函数 v21）

> 后续班级管理升级已推进到前端 v29 / 云函数 v22，最新包和步骤见 `CLASS_MANAGEMENT_DEPLOYMENT.md`。下文保留基础学生管理的权限与操作说明；升级班级功能时不要再上传旧v21/v28包。

本地完成，尚未替您上传 GitHub 或部署腾讯云。请先云函数、再前端。

## 1. 云函数

上传 `CCMTEACHER-deploy-v21-students.zip`，选择 ZIP 包上传。ZIP 根目录直接是 `index.js`、`students.js`、`package.json`、`package-lock.json`、`node_modules/`，没有额外的 src 层。

- 运行环境维持 Node.js 20，执行方法仍是 `index.main_handler`。
- `COS_BUCKET=ccm2025-1311686407`。
- `COS_REGION=ap-chongqing`：这是桶的地域；函数本身在广州不影响。
- `ADMIN_PIN_KEY=system/adminPIN.json`（原管理员账号不变）。
- 其余原有材料/设置环境变量保持不变。`STUDENT_PIN_KEY` 不再使用，不必删除旧文件。
- 等控制台明确显示更新成功，再测试。更新失败时运行的仍是旧函数。

### 权限与私有数据

沿用现有函数执行角色，确保对上述桶具备 `cos:GetBucket`、`cos:GetBucketVersioning`，以及下列前缀的 `cos:GetObject`、`cos:PutObject`、`cos:PutObjectACL` 权限；管理员 PIN 仍需读取权限。已有材料权限保持不变，不要改成全桶公开写入。

新增前缀：

- `system/student-roster-v1/revisions/`：学生名单的私有版本快照，包含邀请码。
- `system/analytics-students-v1/`：正式学生行为记录。
- `system/quiz-results-students-v1/`：正式学生成绩。

不需要手工创建这些目录或 JSON。首次进入名单为空；首次保存由函数创建私有对象。不要把名单、邀请码或成绩上传到公开 GitHub。不要为这些前缀配置允许匿名读取的桶策略；对象会明确设置 `ACL: private`，但额外的公开桶策略仍需由您检查。

为避免两位管理员同时操作覆盖名单，使用不可覆盖的递增版本文件。此机制要求桶未启用过版本控制；若函数提示“当前存储桶有版本控制”，先停止保存操作并反馈，另行配置兼容方案，**不要为此擅自关闭版本控制**。读取名单无需该写入检查。若存储桶策略允许匿名列举对象，也建议限制列举权限。

## 2. 前端

把 `CCM-FRONTEND-2026-09-03-v28.zip` 解压内容覆盖 GitHub CCM 仓库对应路径。保留线上已有 PDF.js、课程页面和材料目录；这个包是增量前端包，不是完整网站备份。

PWA 文件仍包含在包内；服务工作线程继续只缓存中性的离线说明页，不缓存名单、邀请码、成绩或课程材料。

刷新后重新登录管理员：系统设置 → `④ 学生管理 / Students`。

## 3. 使用与验收

1. 手动新增一个真实学号，中文名或英文名至少填写一项。学号作为文本保存，保留开头的 0，英文字母统一大写，支持全角转半角。
2. 云端生成6位邀请码，每位只使用1–9，当前名单内不重复；点击“复制信息”私发给学生。
3. 模板含2行示例，请删除或替换。维持三列表头：学号、中文名、英文名。学号列按文本填写，避免 Excel 丢掉前导零或长数字。支持 XLSX/CSV，单文件不超过2MB，每批最多300人。
4. 导入先预览，再确认；无姓名、重复学号、公式等问题会阻止导入。不要在模板多余行放备注。
5. 编辑只改姓名，不改学号。停用保留历史成绩；重置邀请码后请重新复制登录信息。旧码再次请求云端时失效，不会远程关闭已打开的网页或撤销已公开的课程链接。
6. 用学生学号与邀请码登录，顶部显示姓名和学号。答题后，教师看板的测试表和 CSV 包含学号、姓名、得分。
7. 查看全部邀请码/复制全部信息仅供管理员使用；不要把所有人的邀请码发到班级群。

旧 studentPIN 测试账号不读取、不迁移；原测试成绩/行为文件保留在旧路径，但不混入新统计。管理员 PIN 不变。用户在前端保存的旧登录状态也会失效，需要重新登录。

CSV 用 Excel 打开时若需要保留前导零，请用“从文本/CSV导入”并把学号列指定为文本。

## 4. 已做检查与限制

- `node tools/test-students.cjs`：姓名/学号验证、唯一邀请码、登录/停用/重置、重复整批拒绝、并发冲突、版本控制防护、管理员鉴权和旧学生账号拒绝、前端导入解析与邀请文案。
- `node tools/check-inline-scripts.js index.html`、前后端 JavaScript 语法检查、`node tools/test-pwa.cjs` 通过。
- 浏览器本地模拟测试：新增仅英文名学生、全角学号规范化、前导零保留、编辑姓名且学号只读。
- 尚需部署后验收真实 COS 权限、网络请求、Excel 实际导入和学生答题统计；未向生产环境写入测试名单。
- 此版本是小班教学的简易账号方案（学号＋邀请码）；不是完整的密码/会话管理系统。

本地测试工具 `tools/student-ui-harness.html` 使用假数据，不能部署为正式学生管理入口。
