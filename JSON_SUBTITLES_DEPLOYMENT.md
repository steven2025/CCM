# 多语言JSON字幕（前端v30 / 云函数v23）

新增通用的多语言字幕JSON链接登记和替换，兼容现有各语种独立SRT。没有修改或覆盖线上材料目录，也未删除任何COS文件。

## 部署

1. 原云函数上传 `CCMTEACHER-deploy-v23-json-subtitles.zip`，执行方法仍为 `index.main_handler`，Node.js 20。ZIP根目录包含index.js、students.js、subtitle-json.js及原依赖，没有src外层。
2. 更新成功后，将 `GITHUB发布页/` 内部文件上传到CCM仓库根目录；同名覆盖、其他课程文件保留。或使用 `CCM-FRONTEND-2026-09-03-v30.zip`。
3. 主要前端改动是 `index.html` 和新文件 `assets/subtitle-json.js`，务必两者都上传。
4. 桶、地域、函数地址不变；沿用现有对课程对象的GetObject/HeadObject及材料目录读写权限。无需新增桶和手工配置JSON。若角色权限限定了目录，须确保可读取相应 `chapter1/` 或未来的视频字幕路径。

## 添加“0. 盈塑工业（YingSu）”

1. 管理员 → 系统设置 → 新增文件 → 第1章 → 视频案例 → “＋登记 COS 视频”。
2. 点击 **填入“0. 盈塑工业（YingSu）”链接**。名称、视频地址、字幕JSON地址会自动填入，位置自动选“添加到首位”。
3. 点击“验证并登记”。云函数检查对象和字幕格式后，保存新视频，不改动原有视频名称。相同视频链接已在本章存在时会阻止重复登记，请编辑原条目。
4. 新视频仍遵循原规则：**默认隐藏**。到“开放与关闭”开放此视频并保存，学生端才显示。新JSON语种默认开放，但被视频本身的隐藏状态统一挡住。

已预填的地址：

- 视频：`https://ccm2025-1311686407.cos.ap-chongqing.myqcloud.com/chapter1/yingsu.mp4`
- 字幕：`https://ccm2025-1311686407.cos.ap-chongqing.myqcloud.com/chapter1/yingsu_subtitles.json`

实际文件经过新解析器只读验证：中文、英语、印尼语、韩语、马来语、泰语，每种161条，结束359.2秒，默认中文。原JSON不需拆分或重做。

## 后续视频的字幕维护

- 登记任何新视频时，都可填可选的多语言JSON链接。
- 已有视频旁边的 **JSON字幕链接** 按钮，可以登记或替换字幕JSON。也可以继续使用原“上传SRT”和逐语种“替换为SRT”。
- 每个JSON语种在目录中显示为单独字幕项，可重命名、排序、开放/隐藏，播放时只列出开放语种。
- 更换JSON时，会替换其中的同语种字幕（包括该语种原有的SRT），保留原显示名称和开放/隐藏状态；旧JSON中已删去的语种会移出目录；与新JSON不同语种的独立SRT保留。新增语种默认开放。保存前请确认此更新范围。
- 用独立SRT替换某个JSON语种后，该语种按SRT加载，其他JSON语种不变。
- “移除本视频全部JSON字幕”只移除JSON字幕登记，保留独立SRT和视频，不删除COS原文件。目录仍按原逻辑保存备份和校验revision。
- 同一个COS链接覆盖更新内容后，也应到此入口验证并保存，才能同步新增/移除语种。已打开的播放窗口请关闭后重新打开；窗口内会复用一次读取的JSON，以便快速切换语言。
- 永久链接须为本课程COS桶中的HTTPS .json地址，不带临时签名、查询参数或片段；不允许把system配置目录作为字幕来源。浏览器跨域读取须在COS中允许课程站点。

## 支持的文件格式

```json
{
  "meta": {
    "defaultLanguage": "zh",
    "languages": { "zh": "中文", "en": "English" }
  },
  "subtitles": {
    "zh": [{ "start": 0.3, "end": 2.4, "text": "中文字幕" }],
    "en": [{ "start": 0.3, "end": 2.4, "text": "English subtitle" }]
  }
}
```

start/end必须为数字秒数，每条end大于start，按时间排序且不重叠；文字不能为空。文件最大5MB，1–30种语言，总字幕最多30000条。额外的index/startTime/endTime等字段可保留，播放器使用start/end/text。zh/CHN→ZH，ko→KR，ms→MY，ja→JP，lo→LAO，其余语种代码转大写。

字幕按纯文本显示，不执行JSON文字中的HTML。语种隐藏是界面显示控制：同一公开JSON中仍包含全部语种，不等于对单语种内容进行保密隔离。

## 检查结果与上线验收

- `node tools/test-subtitle-json.cjs`：格式/时间/语种别名、管理员鉴权、本桶URL限制、验证不写入、目录版本冲突、首位登记、SRT共存/隐藏语种/移除不删文件、实际播放器加载代码的语言切换/缓存/纯文本显示测试通过。
- 学生班级管理、PWA和内联脚本回归检查通过；无新增依赖。
- 用提供的实际盈塑JSON完成六语种解析检查。尚未对线上目录执行新增或修改，也未完成线上音画同步验收；部署并登记后请播放核查，并切换中文、英语和其他语种。
